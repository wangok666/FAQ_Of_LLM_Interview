1. 对于sglang,vllm,LMdeploy原理和框架了解吗?
2. 模型部署和推理优化方面的一些经验
3. qwen3-4B 部署起来,微调起来占据显存大小
4. 模型怎么量化部署?
5. 怎么多卡部署?


---

qwen-4b

部署:
可以量化
```python
from awq import AutoAWQForCausalLM
model.quantize(tokenizer, quant_config=quant_config)

model.save_quantized(quant_path)
tokenizer.save_pretrained(quant_path)
```

更换量化方法为 GGUF,, 或者量化精度优化


```shell
# 在单张 24G 显卡上部署 32B 模型 注意 quantization awq
python -m vllm.entrypoints.openai.api_server \
    --model /path/to/Qwen2.5-32B-Instruct-AWQ \
    --quantization awq \
    --dtype half \
    --max-model-len 16384 \
    --gpu-memory-utilization 0.95 \
    --port 8000
```

推理:
1.KV Cache 的内存与计算优化
PagedAttention-模仿操作系统的虚拟内存分页，将 KV Cache 存储在不连续的物理空间。
block_size=16。将 Cache 分为 16 个 token 一组的块。
gpu_memory_utilization=0.9。手动控制模型占用的显存比例，预留足够空间给 KV Cache 块，防止因碎片化导致的 OOM。

2.KV Cache 复用 (Prefix Caching): 
enable_prefix_caching=True
我们的 Agent 场景中，System Prompt（角色设定、工具定义）是固定的。开启此项后，多轮对话中相同的前缀只计算一次 KV，直接命中缓存，显著降低 TTFT



### 怎么多卡部署?

* **并行策略**：使用 Tensor Parallelism (TP) 和 Pipeline Parallelism (PP)。对于 vLLM，通常建议将 `tensor_parallel_size` 设置为总显卡数（即 8）。
* **显存分配**：
* 模型权重：$72B \times 2 \text{ bytes} \approx 144\text{GB}$。
* 平均每张显卡分摊：$144\text{GB} / 8 = 18\text{GB}$。
* 剩余空间（每卡约 22GB）将用于存放 KV Cache，足以支持长文本推理。

#### 2. 环境准备

确保两台机器可以互相通过 SSH 免密访问，并且安装了相同版本的依赖：

```bash
pip install vllm ray
```
#### 3. 启动 Ray 集群

需要在两台机器上建立 Ray 的主从关系。

**在机器 A (Head Node) 执行：**

```bash
ray start --head --port=6379
```

**在机器 B (Worker Node) 执行：**
（将 `<Head-Node-IP>` 替换为机器 A 的内网 IP）

```bash
ray start --address='<Head-Node-IP>:6379'
```

#### 4. 使用 vLLM API Server (推荐)

为了更方便地对接前端或应用，建议启动兼容 OpenAI 接口的服务端：

```bash
# 在 Head Node 执行
python -m vllm.entrypoints.openai.api_server \
    --model Qwen/Qwen2.5-72B-Instruct \
    --tensor-parallel-size 8 \
    --distributed-executor-backend ray \
    --host 0.0.0.0 \
    --port 8000
```

vLLM 会自动检测 Ray 集群并分发模型分片

--distributed-executor-backend ray

可以考虑使用 **GPTQ** 或 **AWQ** 4-bit 量化版本的 Qwen-72B

多机 TP 并行对节点间的网络要求极高。如果两台机器之间不是 InfiniBand (IB) 互联，而是普通的万兆网，推理延迟（Latency）会因为跨机通信而大幅增加。

